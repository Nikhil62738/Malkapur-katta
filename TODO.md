# TODO
- [ ] Replace TrendingReels YouTube-based reel modal with local MP4 reels from `/Reels` folder.
- [ ] Update reel data (content.ts / contentMr.ts) to reference local mp4 URLs and titles matching filenames.
- [ ] Implement LocalVideoModal using HTML5 `<video>`.
- [ ] Update TrendingReels to use local reel items (video path) instead of `youtubeId`.
- [ ] Run `npm run dev` and verify Trending Reels plays correct local mp4s.
- [ ] Run `npm run build` to ensure TypeScript passes.

